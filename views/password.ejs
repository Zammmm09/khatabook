<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Enter Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-100 font-[Helvetica]">

  <!-- Navbar -->
  <nav class="flex px-10 justify-between py-5 bg-white shadow">
    <h3 class="text-2xl tracking-tight font-semibold">Khaatabook</h3>
    <div class="flex gap-5 items-center">
      <a href="/" class="text-blue-600 hover:underline">Home</a>
      <a href="/create" class="text-blue-600 hover:underline">Create New Hisaab</a>
    </div>
  </nav>

  <!-- Password Prompt Box -->
  <div class="flex justify-center items-center min-h-[80vh] px-4">
    <form
      action="/verify-password/<%= id %>"
      method="POST"
      class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md space-y-6"
    >
      <h2 class="text-2xl font-semibold text-center text-gray-800">🔐 Password Protected Note</h2>
      <p class="text-gray-500 text-center text-sm">This note is protected. Enter the password to view it.</p>

      <input
        name="password"
        type="password"
        placeholder="Enter password"
        required
        class="w-full px-4 py-2 rounded-md bg-zinc-200 text-base"
      >

      <button
        type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded-md transition duration-200"
      >
        Unlock Note
      </button>
    </form>
  </div>
</body>
</html>
